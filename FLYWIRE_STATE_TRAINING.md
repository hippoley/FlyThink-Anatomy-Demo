# FlyWire 多轮状态训练：第二轮实验

本轮的核心仍是实际 FlyWire v783 子图：512 个真实神经元、9,692 条连接。
网络内部连接增益参与反向传播，连接索引来自上一轮校验过的官方连接表。
训练脚本同时校验原数据摘要与子图文件摘要，没有随机网络替代路径。

## 本轮实际做了什么

- 从整段历史拼接分类，改成逐轮输入、跨轮保留神经隐藏状态。输入只有当前
  话语特征，模型不会读取正确槽位、正确指代或正确历史状态。
- 每轮输出：12 个槽位的值、暂停标记、指代焦点、OOD 待处理标记、8 类状态操作。
- 覆盖 3 个房间、灯与窗户、开关与数值槽位；每条生成对话 8 轮。
- 480 条训练对话；96 条使用不同表达模板的测试对话；6 条手写验收对话。
- 同种子、同训练语料、同轮数、同优化器，分别训练真实可训练连接、冻结连接、
  重排连接、无跨神经元循环连接四组。每组 40 轮 CPU 训练。
- 四组模型均已保存权重，并验证重新加载后的输出一致。

## 测试结果：尚未通过核心验收

以下是每轮完整状态全部正确的比例，不是单字段平均分。完整状态包括槽位、
暂停、焦点与 OOD 标记；整条对话指标还要求每轮操作预测正确。

| 模型 | 不同表达模板，768 轮 | 手写验收，24 轮 |
|---|---:|---:|
| 真实连接，训练内部增益 | 17.19% | 37.50% |
| 真实连接，冻结内部增益 | 17.32% | 37.50% |
| 重排连接，训练内部增益 | 17.71% | 41.67% |
| 无跨神经元循环连接，保留自身泄漏记忆 | 18.75% | 33.33% |

**四组的整条对话完全正确率都是 0。当前没有真实拓扑优越性的证据。**
真实网络的全部 9,692 个内部增益有更新，但这不是任务有效性的证明。
重排对照随机置换边的终点，重复边会合并累加；不是严格无重边的保度重连。
目前只有一个随机种子，不能对小幅分差作统计结论。

真实网络清空跨轮隐藏状态后，模板测试的完整状态正确率反而达到 20.96%，
说明目前记忆也可能带来干扰；不能声称持续记忆已经学会。

## 复现到的关键失败及提交约束

真实模型依次看到：

1. 打开客厅灯。
2. 主卧窗户打开。
3. 好的我知道了。

第三轮虽然识别出 `retain`，预测的整张表仍漏掉了客厅灯。
因此新增 `intent_state_commit.py`：网络提供候选，持久树只接受增量；遗漏不能
删除节点。`retain` 完整保留此前状态。撤回只移除已解析焦点，全撤回还要通过
明确指令检查。不同 OOD 候选保留各自原文并挂载，当前不调用生成模型。

`persistence-replay.json` 同时记录原始神经预测、提交决定和实际持久树。
保留约束来自确定性运行时，**不能把它算作神经网络学会了记忆**。这层提交边界
依然不能保证错误新增、错误数值或错误指代全部被拦截；不接真实设备执行。
未识别的撤回表达需要澄清，不应强行删除。复杂跨房间指代仍未完成。

## 本地复现

先按 `FLYWIRE_TRAINING.md` 安装 CPU PyTorch；仓库已包含校验后的子图。

```bash
python scripts/train_flywire_state.py --epochs 40
python -m unittest discover -s tests -p 'test_flywire_state.py'
python scripts/infer_flywire_state.py '打开客厅灯' '主卧窗户打开' '好的我知道了' '全部撤回'
```

`artifacts/flywire-state/report.json` 提供完整指标、逐轮手写失败记录和权重摘要。
`corpus.json` 由脚本确定性生成。`.pt` 为本地训练权重，CI 手动运行也会上传。
网络当前是连接组约束的 rate RNN，不是生物脉冲仿真或全脑训练。

## 下一轮训练的验收重点

先在训练集与独立开发集上定位状态损失，改为直接监督增量操作及其指向对象，
并增加对话长度、房间切换和引用对象的组合。当前测试集保留为固定回归集；
若据此改语料或模型，必须另建封存测试集，不再把它称为未见测试。
继续保持相同预算的真实拓扑与对照实验。生成式 OOD、全部 46 个物模型、
ASR 打断、多人权限和全脑扩展尚未由本实验验证。
